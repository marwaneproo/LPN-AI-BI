# Export Query Catalog

This folder contains Oracle SQL query templates that Youssef runs manually on the company PC against Compiere / Oracle 11g.

Rules:

- One `.sql` file per exported table.
- Save output as CSV with a header row and UTF-8 encoding.
- Document every date or status filter in `manifest.json`.
- Do not run these queries from this project machine.

